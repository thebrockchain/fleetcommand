# VO session state: 2026-08-17, ~1am

Paused by Brock's call: **new XLR cable first, then record the real takes.**
The cable in the rig is faulty (mic-end connector, full-scale crackle when
flexed: full conviction written in `~/Podcast/notes/NEXT.md` and the
podcast-workspace memory). Tonight's takes went through the bad cable with the
connector taped still; they are references, not keepers.

## What exists

- `blockA-take4.wav`: best take of the night. Peaks -9.5 dB, floor ~-35,
  no crackles, three clean line-chunks with gaps. NOT locked; Brock paused
  before judging it. Its `-voice.wav` is the mono channel-2 extract, the
  `.mp3` is the chat-playback copy.
- Takes 1 to 3: level-hunting. Take 2 and 3 peak near clipping. Keep for nothing.

## The block plan (record these when the new cable is in)

Lines live in `../VIDEO-SCRIPT.md`, split into blocks A, G (A = hook ×3,
B = crew + bakery, C = market lens, D = AUDIT/MEDIC/honesty, E = gate +
second risk, F = send back / approve, G = why real + close). Protocol that
worked: 2 s stillness first, full breath between lines, record all 4 channels
24-bit off the Scarlett (list avfoundation devices first: the index moves),
keep channel 2, measure every take before accepting, "go again" on demand.

## Rig state left behind

- Mic gain knob is at **~2 o'clock** (moved down twice from 3:30 during level
  hunting; at 2:00 Brock's performance peaks land ≈ -9.5 dB). The podcast
  SETUP.md "do not move the gain knob" note refers to a dial-in that no longer
  exists. **Re-dial after the cable swap**: the floor and levels both change.
- The mic-end XLR connector is taped to the boom arm. Remove tape with the
  old cable.

## Also owed before the video ships

- **RESOLVED 2026-09-03.** The film exists and shipped. It was never lost, only
  absent from THIS Mac. `~/Desktop/fleetcommand-demo.mp4` and the 4K
  master do not exist anywhere on disk despite VIDEO-SCRIPT.md saying "THE FILM
  IS CUT". It must be re-rendered with `record.mjs` + `edit.mjs`; a re-render
  can move the beats (marks.json), which is exactly why the VO is recorded as
  per-line blocks: the merge places each line at its mark wherever it lands.
- Merge VO onto film, upload public to YouTube, register + submit on Devpost
  (ENTRY-DEVNETWORK.md, plus SerpApi and name.com side tracks).

## Status, 2026-09-03

The narrated film is public on YouTube and now downloadable from the press kit
at files.thebrockchain.com/fleetcommand/fleet-command-narrated.mp4 (720p, the
YouTube transcode, 15,434,083 bytes, 2:46). The 1080p master
(fleetcommand-NARRATOR-music.mp4, 38,095,709 bytes) still lives only on
Brockchains-MBP and is not in R2.

The takes below are still the faulty cable references. Blocks B to G were never
recorded, because the shipped narration is gemini TTS (voice Charon), not
Brocks voice. Recording the real reads is still open.
